name: Ignore Docs Changes

on:
  push:
    branches:
      - main
      - 'release/*'

    paths-ignore:
      - '*.md'
      - 'docs/**'

jobs:
  ignore-docs-job:
    runs-on: ubuntu-latest

    steps:
      - name: Workflow Triggered
        run: echo "Non-doc files changed"